export const metadata = {
  description: 'Explore all the decentralized certificates that you can apply for on TheBadge',
}
export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
