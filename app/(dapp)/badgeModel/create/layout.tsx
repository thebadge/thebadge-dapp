export const metadata = {
  description: 'TheBadge allows you to create certification models and get pay by it',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
